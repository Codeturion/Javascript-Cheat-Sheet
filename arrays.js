let selectedColors = ['red','blue']; //JS is dynamic so typeof values can change array is an object in JS
selectedColors[2] = 'green';
selectedColors[3] = 1; //Array can have more than one typeof object unlike c#
console.log(selectedColors); //same as c#